name: Preprocessing Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'Eksperimen_SML_Yusuf-Saputrah/dataset_raw.csv'
      - 'Eksperimen_SML_Yusuf-Saputrah/preprocessing/automate_Yusuf-Saputrah.py'

jobs:
  preprocess:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12.7'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas scikit-learn

      - name: Run preprocessing script
        run: |
          cd Eksperimen_SML_Yusuf-Saputrah/preprocessing
          python automate_Yusuf-Saputrah.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add Eksperimen_SML_Yusuf-Saputrah/preprocessing/dataset_preprocessing.csv
          git commit -m "Automated data preprocessing update" || echo "No changes to commit"
          git push
